"""Namespace package the registry iterates over to discover instrument plugins.

Each direct subpackage here is one instrument and must export a module-level
``INSTRUMENT`` instance (see :mod:`engine.instruments.base`).
"""

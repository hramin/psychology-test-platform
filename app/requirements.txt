# Web tier
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
gunicorn>=22.0
jinja2>=3.1
python-multipart>=0.0.9

# Data
sqlalchemy[asyncio]>=2.0.30
asyncpg>=0.29
alembic>=1.13
pydantic-settings>=2.2
redis>=5.0

# Auth
argon2-cffi>=23.1   # argon2id password hashing (username/password login + reset)

# Tasks / notifications
celery>=5.3
kavenegar  # SMS/OTP SDK (imported lazily; only the worker needs it at runtime)

# HTTP / testing
httpx>=0.27
pytest>=8.0
pytest-asyncio>=0.23
fakeredis>=2.20   # in-memory Redis for tests (OTP / rate-limit / sessions)
aiosqlite>=0.19   # async SQLite for identity DB tests (no Postgres needed)
